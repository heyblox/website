# Public GET URL for the Blox Fraud Index (cn-marketing HTTP API).
# Resolve from the cn-marketing-prd CloudFormation output HttpApiUrl + /fraud-index,
# or from FRAUD_INDEX_PUBLIC_URL after the marketing service deploy.
# Example: https://{api-id}.execute-api.us-east-2.amazonaws.com/fraud-index
FRAUD_INDEX_API_URL=
